# Dependencies for projects/ai_agents
# Install with: pip install -r projects/ai_agents/requirements.txt

# Shared by every agent
anthropic>=0.40.0
python-dotenv>=1.0.0

# Agent 1 (web_search) only — a no-API-key-required web search backend
ddgs>=9.0.0
